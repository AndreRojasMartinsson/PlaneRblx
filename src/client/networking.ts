import { GlobalEvents } from "shared/networking/events";
import { GlobalFuncs } from "shared/networking/functions";

export const Events = GlobalEvents.createClient({});
export const Funcs = GlobalFuncs.createClient({});
